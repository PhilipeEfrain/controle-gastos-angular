rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Bloqueio preventivo completo: o aplicativo não utiliza Storage no cliente
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
